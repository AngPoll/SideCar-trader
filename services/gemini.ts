// services/gemini.ts
export async function geminiPrompt(prompt: string) {
  // TODO: Replace with real Gemini API call (should be done on a server, not in the browser)
  return `Gemini placeholder response for: ${prompt}`;
}
